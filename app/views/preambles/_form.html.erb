<%= form_for(@preamble) do |f| %>
  <% if @preamble.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@preamble.errors.count, "error") %> prohibited this preamble from being saved:</h2>

      <ul>
      <% @preamble.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <!-- <div class="field">
    <%= f.label :meta %><br>
    <%= f.text_field :meta %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
