<%= form_for(@consideration) do |f| %>
  <% if @consideration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consideration.errors.count, "error") %> prohibited this consideration from being saved:</h2>

      <ul>
      <% @consideration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <!-- <div class="field">
    <%= f.label :meta %><br>
    <%= f.text_field :meta %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
